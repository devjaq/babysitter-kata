Babysitter Kata Guide

This project requires nodeJS.

The only dependency used here is Mocha for testing. Once the repository is downloaded, this can be installed using npm with the terminal command "npm install".

This program should be run using a modern web browser and works via GUI.

The tests can be run in the command line using the command "npm test".